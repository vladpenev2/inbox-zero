[build]
builder = "dockerfile"
dockerfilePath = "docker/Dockerfile.prod"

[deploy]
# Removed healthcheckPath - the /api/health endpoint requires HEALTH_API_KEY header
# which Railway's built-in healthcheck doesn't support
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
